code english

func int fibonacci(int n) returns int [:
    if n <= 1 [:
        return n
    :]
    return fibonacci(n - 1) + fibonacci(n - 2)
:]

int i = 0
while i < 10 [:
    print("fib(" + i + ") = " + fibonacci(i))
    set i = i + 1
:]
